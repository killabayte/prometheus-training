IAM role:
AmazonEC2ReadOnlyAccess

User data script:
#!/usr/bin/env bash
wget -O - https://raw.githubusercontent.com/killabayte/prometheus-training/master/scripts/2-install-grafana.sh | bash

Alter Prometheus config:
/etc/prometheus/prometheus.yml

  - job_name: 'ec2_nodes'
    ec2_sd_configs:
      - region: us-east-1
        access_key: PUT_THE_ACCESS_KEY_HERE
        secret_key: PUT_THE_SECRET_KEY_HERE
        port: 9100
    relabel_configs:
      - source_labels: [__meta_ec2_tag_Name]
        regex: Prometheus.*
        action: keep
      - source_labels: [__meta_ec2_public_ip]
        regex:  '(.*)'
        target_label: __address__
        replacement: '${1}:9100'

Generate load:
dd if=/dev/zero of=/dev/null

Query grafana:
100 - (avg by (instance) (irate(node_cpu_seconds_total{job="ec2_nodes",mode="idle"}[5m])) * 100)
